{
  "name": "opencode-tmux",
  "private": true,
  "type": "module",
  "scripts": {
    "check:env": "command -v bun >/dev/null && command -v tmux >/dev/null",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "bun-types": "^1.3.6",
    "typescript": "^6.0.2"
  }
}
